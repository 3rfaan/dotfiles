require("nightfox").setup({
    options = {
        styles = {
            comments = "italic",
            conditionals = "bold,italic",
            constants = "bold",
            functions = "bold,italic",
            keywords = "bold,italic",
            strings = "italic",
            types = "bold",
        }
    }
})
