require("nightfox").setup({
    options = {
        styles = {
            comments = "italic",
            conditionals = "bold",
            constants = "bold",
            functions = "bold",
            keywords = "bold",
            strings = "italic",
            types = "bold,italic",
        }
    }
})
